# Zetasphere-Histogram-analysis
Small app to generate publication ready histograms from raw zip files generated by ZetaSphere (Zetaview). It can overlap different samples/channels, perform statistics, and also detect different populations by applying a Gaussian Mixture Model.
